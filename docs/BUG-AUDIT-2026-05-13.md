# Bug-Audit 2026-05-13 — laufendes Tracking

**Lebendes Arbeitsdokument** während eines mehrrundigen Code-Audits. Wenn alle Funde gefixt oder bewusst akzeptiert sind, kann das Doc archiviert oder gelöscht werden.

Schweregrade:
- **🔴 Kritisch** — blockiert Feature-Arbeit, Datenkorruption, Security, oder Endpoint-broken
- **🟠 Hoch** — wichtige UX-Bugs, Doku-Drift mit operationalen Konsequenzen, i18n-Verstöße
- **🟡 Worth-fixing** — soll gefixt werden, aber nicht ship-blockierend
- **🟢 Hinweis** — beobachten, evtl. später adressieren

Status:
- **VERIFIED** — bestätigt durch ≥ 2 unabhängige Reviewer
- **VERIFIED-NUANCE** — bestätigt, aber mit Detail-Anpassung
- **DISPUTED** — zwischen Reviewern uneinig
- **OPEN** — neuer Fund, noch nicht verifiziert
- **FIXED** — gefixt, im Code

---

## 🔴 Kritisch (21)

| # | Datei:Zeile | Was | Runde | Status |
|---|---|---|---|---|
| 1 | `backend/app/main.py:2303, 2316, 2369, 2381` (audit-Zeitstand) | `supabase` ohne Modul-Import → DELETE `/api/images/{id}` und POST `/api/images/generate` (chat-anchored) = 500. Audit-Scope unterschätzt — POST-Pfad in Phase-A 2026-05-18 entdeckt | 1 | FIXED 2026-05-18 |
| 2 | `backend/app/image_gen.py:341–349` | Daily-Cost-Cap zählt nur `succeeded`, ignoriert `estimated_cost` der laufenden Anfrage; parallele Requests passieren beide | 1 | VERIFIED |
| 3 | `backend/app/image_gen.py:67–87` | `app_user_limits`+Setting-Lookup ohne try/except → 500 auf PROD bis Migration angewendet | 1 | VERIFIED |
| 4 | `backend/app/main.py:2024–2218`, `_stream_pool_response` | `send_pool_message` keine `audit.log_event`-Aufrufe (vgl. `send_message` L801/861) — verstößt gegen CLAUDE.md:113 | 1 | VERIFIED |
| 5 | `backend/app/main.py:2202–2208, 2247–2253` | Pool-Chat `record_usage(chat_id=None)` — Token-Usage verliert Pool-Chat-Dimension komplett | 1 | VERIFIED |
| 6 | `frontend/src/App.jsx:418–437` + `PoolDetail.jsx:209–220` | Stream-Done-Callback überschreibt mid-Stream gewechselte Konversation mit alten Daten | 1 | VERIFIED |
| 7 | `frontend/src/App.jsx:689` + Section-Handling | Sidebar zeigt Chat-Liste während Bilder-Tab; klick navigiert zurück ohne `activeSection`-Reset | 1 | VERIFIED |
| 8 | `docs/PROD-UPGRADE-PLAYBOOK.md:99–106, 292–311` | Listet nur A1+A2 als ausstehend; fehlen `20260512_pool_chat_messages_*`, `20260513_a_image_generation`, `20260513_b_widen_source_check` | 1 | VERIFIED |
| 9 | `backend/app/encryption.py:13` | Fernet-Key = `sha256(JWT_SECRET)` — JWT-Rotation invalidisiert silent alle Provider-Keys; `providers.py:36` fällt silent auf Env-Var-Keys zurück | 2 | VERIFIED |
| 10 | `backend/app/main.py:836–884, 2233–2270` | Partial-Write-Orphan: LLM-Fehler mid-stream → User-Message persistiert, Assistant-Message NICHT, kein Token-Usage, kein Audit, kein `done`-Event | 2 | VERIFIED |
| 11 | `backend/app/config.py:29` | `RATE_LIMIT_STORAGE_URL` Default `"memory://"` → bei N Workern multipliziert sich das Rate-Limit silent | 2 | VERIFIED |
| 12 | `backend/app/pools.py:443–453, 478–484` | N+1 sync supabase-py in async Handler — 50 Pool-Chats = 50 serielle RTs, blockiert Event-Loop → 502s unter Last | 2 | VERIFIED |
| 13 | `supabase/migrations/20260216_phase_c_assistants_templates.sql:7,22` | `is_global=true`-Rows cascade-deleted wenn Admin-Owner gelöscht wird | 2 | VERIFIED |
| 51 | `backend/app/rag.py:298–308` | **Infinite Loop in `chunk_text`** wenn Breadcrumb-Prefix ≥ `chunk_size − overlap` — friert Upload-Worker ein | 3 | OPEN |
| 52 | `backend/app/documents.py:863` + `main.py:1056` | Concurrent same-file Upload bypasst Dedup (Filter `status=ready`); 2× Mistral-OCR-Kosten + doppelte Chunks | 3 | OPEN |
| 53 | `backend/app/rag.py:1329–1334` | **Prompt-Injection** via OCR-Inhalt in `<content>{content}</content>` ohne `html.escape` | 3 | OPEN |
| 54 | `backend/Dockerfile:48` + `main.py:157–168` | **Rate-Limit-Bypass via gespooftem `X-Forwarded-For`**: `FORWARDED_ALLOW_IPS=*` Default + Login-Rate-Limit-Key über `get_remote_address` | 3 | OPEN |
| 55 | `backend/app/documents.py:965, 992, 1011` | **PostgREST-Filter-Injection** via ungeprüftem `chat_id`-String in `.or_(f"...")` | 3 | OPEN |
| 56 | `backend/app/rag.py:579, 627, 667` | **Embedding-Provider-Race**: Admin-Toggle mid-Upload → Vektoren aus verschiedenen Modellen im selben Column | 3 | OPEN |
| 57 | `backend/app/llm.py:251, 278, 301, 382, 411, 455, 498, 539` | Provider-Error-Body wortwörtlich in `LLMError` → bubbled an Client → Prompt-Leak | 3 | OPEN |

## 🟠 Hoch / Doku-Drift / i18n (7)

| # | Datei:Zeile | Was | Runde | Status |
|---|---|---|---|---|
| 14 | `docs/IMPLEMENTIERT.md` | Kein Eintrag für `20260513_b_widen_source_check.sql` | 1 | VERIFIED |
| 15 | `docs/ANWENDER-DOKUMENT.md:55` | Behauptet Sidebar auto-closet; gilt nur mobile (`App.jsx:65–75` matchMedia-guard) — Doku muss differenzieren | 1 | VERIFIED-NUANCE |
| 16 | `docs/ANWENDER-DOKUMENT.md:26` | Flowchart „PDF/TXT/Bild" — real 11 Filetypes | 1 | VERIFIED |
| 17 | `backend/app/main.py:175–181` | Keine Security-Headers (HSTS, X-Frame-Options, CSP, X-Content-Type-Options) — Clickjacking-Risiko | 2 | VERIFIED |
| 18 | `backend/app/pools.py:268–294` `use_invite_link` | TOCTOU-Race auf `max_uses` — kein Schema-Constraint | 1+2 | VERIFIED |
| 19 | `frontend/src/components/AdminDashboard.jsx:7–15, 477–482` | 6 von 9 Tab-Labels + 6 Stats-Labels hartkodiert deutsch — i18n-Regel verletzt | 2+3 | VERIFIED |
| 58 | `backend/app/llm.py:_stream_anthropic` | Niemals `stop_reason` ausgewertet — `max_tokens`-Truncation wird unsichtbar als komplette Antwort gespeichert | 3 | OPEN |

## 🟡 Worth-fixing (38)

| # | Datei:Zeile | Was | Runde | Status |
|---|---|---|---|---|
| 20 | `backend/app/main.py:248–270` | Refresh-Token wird nicht rotiert — 7-Tage Replay-Window | 2 | VERIFIED |
| 21 | `backend/app/auth.py:53–60` | `jwt.decode` ohne `leeway` — Clock-Skew → spurious 401 | 2 | VERIFIED |
| 22 | `backend/app/rag.py:1166–1173` `enrich_with_neighbors` | Keine Defense-in-Depth-Tenancy-Filter | 2 | VERIFIED |
| 23 | `backend/app/image_gen.py:62` | Daily-Cap-Window mit `< T23:59:59.999999` schließt letzte Mikrosekunde aus | 2 | VERIFIED |
| 24 | `backend/app/main.py:2381` (audit-Zeitstand, post-fix L2382) | `delete_generated_image` Delete ohne `eq("user_id")` in WHERE — TOCTOU | 2 | FIXED 2026-05-18 |
| 25 | `backend/app/auth.py:107–130` | Concurrent-Registration-Race auf Email/Username | 2 | VERIFIED |
| 26 | `backend/app/auth.py:22–27` + `models.py:54` | bcrypt 4.0.1 schneidet >72 Bytes still ab; `password max_length=128` lässt Differenz entstehen; `LoginRequest` hat sogar **keine** max_length | 1+3 | VERIFIED |
| 27 | `backend/app/documents.py:881–898` | Content-Hash-Dedup race-window während OCR | 1 | VERIFIED |
| 28 | `backend/app/admin.py:218–221` | Non-atomic `is_default`-Toggle (DB-Constraint deckt Korrektheit, hässlicher Fail) | 1 | VERIFIED |
| 29 | `backend/app/providers.py:30–40` | `get_api_key` ohne `eq("is_active",True)`-DB-Filter | 1 | VERIFIED |
| 30 | `backend/app/storage.py:38–64` + `pools.py:420–456` | N+1 `last_message_at` pro Chat | 1+2 | VERIFIED |
| 31 | `frontend/src/components/Modal.jsx:48` | Escape schließt den gesamten Modal-Stack | 2 | VERIFIED |
| 32 | `backend/app/main.py:1036–1046` | Extension-only Filetype-Check, kein MIME-Sniff | 2 | VERIFIED |
| 33 | `backend/app/main.py:215, 231, 240` | `request.client.host` statt `X-Forwarded-For` — Audit-IPs sind Proxy-IPs; `request.client` kann `None` sein → AttributeError | 2+3 | VERIFIED |
| 34 | `backend/app/token_tracking.py:37` | Fallback-Pricing `{1.0,3.0}` für unbekannte Modelle silent — fabrizierte Kosten; widerspricht CLAUDE.md „read app_models" | 2+3 | VERIFIED |
| 35 | `frontend/Dockerfile` | Kein `package-lock.json` Lockfile-Pinning, nutzt `npm install` statt `npm ci` | 2 | VERIFIED |
| 36 | `supabase/migrations/20260218_pools.sql:84–118` | Pool-RPC ohne `match_user_id`-Filter — heute app-layer geschützt, Defense-in-Depth-Lücke | 2 | VERIFIED |
| 37 | `frontend/src/components/Bilder.jsx:65, 109` | „Mehr laden" Pagination off-by-one nach Generate | 1 | DISPUTED (2-vs-2-Reviewer) |
| 38 | `frontend/src/App.jsx:192–197` | `defaultModelId` in useEffect-Dep-Liste; Admin-Default-Edit kann mid-Chat User-Modell-Wahl überschreiben | 1+2 | VERIFIED-NUANCE |
| 39 | `frontend/src/App.jsx:678–685` `onHome` | Lässt `activePoolChatId`, `poolTab`, `loading`, `streamingContent`, `error`, `chatDocuments` stale | 1 | VERIFIED |
| 40 | `frontend/src/components/Bilder.jsx:139` | `setTotal((n) => n - 1)` ohne `Math.max(0, …)` Floor | 1 | VERIFIED |
| 59 | `backend/app/rag.py:861` | Targeted-Retrieval umgeht den Reranker komplett | 3 | OPEN |
| 60 | `backend/app/rag.py:968–969` | Cohere-Rerank-Fail (429/500) fällt nicht auf den sauberen No-Rerank-Sort-Branch zurück | 3 | OPEN |
| 61 | `backend/app/llm.py:386, 307` | `_call_azure` schreibt `None`-Content bei Content-Filter; `_call_google` `AttributeError` wenn `parts` nicht Liste | 3 | OPEN |
| 62 | `backend/app/rag.py:1162–1173` | `enrich_with_neighbors` ist N+1, eigene serielle RPCs pro Top-Hit | 3 | OPEN |
| 63 | `backend/app/rag.py:604–610` | `generate_embeddings` ohne Retry auf 429 (anders als Mistral-OCR) | 3 | OPEN |
| 64 | `frontend/src/components/MessageInput.jsx:84–93` | Kein `maxLength={50000}` clientseitig, Paste >50KB → 422 + Message-State weg | 3 | OPEN |
| 65 | `backend/app/auth.py` | **Kein Password-Reset/Change-Endpoint existiert** | 3 | OPEN |
| 66 | `backend/app/auth.py:107, 115, 143` | Username/Email ohne `.strip().lower()` — Duplicates und Lockout-Bugs | 3 | OPEN |
| 67 | `backend/app/main.py:1081, 1827` | OCR-Fehlermeldung wortwörtlich zum Client (echoed Mistral-Body inkl. signed-Upload-URL) | 3 | OPEN |
| 68 | `backend/app/admin.py:48–52` | `admin/usage` baut Datumsstring aus ungeprüftem `end_date` | 3 | OPEN |
| 69 | `backend/app/main.py:228–232` | `AUTH_LOGIN_FAILED`-Audit speichert `username` wortwörtlich — Passwort-im-Username-Fehler werden permanent gelogt | 3 | OPEN |
| 70 | `backend/app/main.py:2483–2517` | `update_user_limits` verifiziert nicht ob User existiert | 3 | OPEN |
| 71 | `backend/app/pools.py:175–194` | `list_members` ist N+1 pro Member | 3 | OPEN |
| 72 | `frontend/src/api.js:35–49` `authFetch` | Refresh nur auf 401, nicht 403 — revoked-Admin behält Token bis natürlicher Ablauf | 3 | OPEN |
| 73 | `backend/app/pools.py add_member` | UNIQUE-Constraint-Verletzung wird nicht catched → 500 zum User | 3 | OPEN |
| 74 | `backend/app/audit.py:49–58` | Truthiness-Bug: `metadata={}` und `target_id="0"` werden silent gedroppt | 3 | OPEN |
| 75 | `backend/app/encryption.py:18` | `encrypt_value` ohne Version/Envelope — Key-Rotation unmöglich (alte ciphertexte nicht von neuen unterscheidbar) | 3 | OPEN |

## 🟢 Hinweise (15)

- 41 — `audit.log_event` sync I/O im async Event-Loop
- 42 — `assistants.py:50–65` Owner-Bypass auf `is_global=true` nach Admin-Demotion
- 43 — `pools.py:481` N+1 User-Lookups in `get_pool_chat`
- 44 — `auth.py:78` `get_user_by_id` auf jedem Request ohne Cache
- 45 — `image_gen.py:318–323` Modell-Lookup ohne `.limit(1)` nondeterministisch
- 46 — `CLAUDE.md:122` AdminDashboard.jsx LOC 1124→1750 (Doku-Drift)
- 47 — `CLAUDE.md:37` + `TODO.md:3` referenzieren `xqt5-ai-plattform-dri/` — existiert nicht; `basline-code/` existiert workspace-level
- 48 — `TODO.md:3, 73, 124` referenzieren `RAG-VERBESSERUNGSPLAN.md` und `RAG-STATUS.md` — existieren nicht
- 49 — `memory project_xqt5_dev_prod_state.md` fehlt Zeile für `20260512_pool_chat_messages_*`
- 50 — `README.md` ~3 Monate stale, widerspricht UV-Policy in CLAUDE.md
- N+ — `CLAUDE.md:128` sagt „`docs/tests/` is empty" — hat tatsächlich 4 .md + `phase3/`
- N+ — 5+ hartkodierte deutsche `title=`/`placeholder=` in `NavRail.jsx`, `Sidebar.jsx`, `TemplatePicker.jsx`
- N+ — Keine Error-Boundaries in `frontend/src/` — unhandled Render-Error reißt App auf Blank-Screen
- N+ — `pools.get_invite_by_token` von `use_invite_link` nicht aufgerufen — Expiry-Check umgehbar wenn Caller direkt `invite_id` hat
- N+ — `is_first_message` in Pool-Flow vergleicht gegen `"New Chat"` Titel-String — Übersetzte Default-Titel brechen Auto-Naming

## Disputed / Offene Verifikation

- **#37 Bilder-Pagination off-by-one nach Generate**: Round-3-Reviewer split 2:2. Mathe ist je nach Annahme über server-side `ORDER BY created_at DESC` konsistent oder nicht. Verdient eigenständige Rechnung.

## Runde 4 — Neue Funde (~25)

### 🔴 Neu kritisch (7)

| # | Datei:Zeile | Was |
|---|---|---|
| 76 | `backend/app/pools.py:268–294` `use_invite_link` | TOCTOU + lost-write: non-atomic max_uses-Check, ein Single-Use-Link kann beliebig viele User aufnehmen (verschärft #18) |
| 77 | `backend/app/admin.py:218–223` `update_model_config` | Momentanes "no default"-Window zwischen Unset und Set; fallback `google/gemini-3-pro-preview` hartkodiert |
| 78 | `backend/app/providers.py:47–65` `get_provider_config` | Filtert nicht `is_active=True` — nach Soft-Delete bleibt stale endpoint_url; LLM-Call mit env-Fallback-Key gegen falschen Endpoint → cross-tenant-Leak |
| 79 | `backend/app/rag.py:1228–1250` `_apply_contextual_retrieval` | Unbounded `asyncio.gather` über chunk_pairs — 500-Chunk-PDF startet 500 parallele LLM-Calls; jede Exception verschluckt; **keinerlei `record_usage`** → Admin-Usage-Dashboard underreportet stark |
| 80 | `backend/app/main.py:228–232` `AUTH_LOGIN_FAILED` Audit | Speichert getipptes Username **wortwörtlich** im Audit-Log — User der Passwort ins Username-Feld tippt landet permanent mit Passwort im Audit-Log (verschärft #69 von worth-fixing zu kritisch) |
| 81 | `frontend/src/api.js:11–27` + `MessageBubble.jsx:49` | JWT-Token im `localStorage` + `image_url` ohne `javascript:`-Scheme-Validation in `<img src>` und `<a href>` → XSS-via-Provider-URL möglich, jede Reflected-XSS exfiltriert Refresh-Token |
| 82 | `backend/app/llm.py:296, 532` Google-API | API-Key in URL-Query-String (`?key={api_key}`) — leaked in httpx-Tracebacks und Reverse-Proxy-Access-Logs. Google REST API unterstützt `x-goog-api-key`-Header |

### 🟠 Hoch (1)

| # | Datei:Zeile | Was |
|---|---|---|
| 83 | `llm.py` alle 4 Streaming-Provider | `stop_reason` / `finish_reason` wird nirgends ausgewertet — `max_tokens`-Truncation unsichtbar (verallgemeinert #58 von Anthropic auf alle 4) |

### 🟡 Worth-fixing (14)

| # | Datei:Zeile | Was |
|---|---|---|
| 84 | `backend/app/main.py:785, 2194` | Non-Stream-Pfade speichern `rag_sources` NICHT (Stream-Pfade Z. 846, 2243 schon) → Zitierungen verschwinden nach Reload für nicht-gestreamte Antworten |
| 85 | `backend/app/storage.py:143` + Migration | `delete_conversation` cascade-löscht alle `app_documents` mit `chat_id` — permanenter Datenverlust ohne Warnung |
| 86 | `backend/app/llm.py:471, 517` `_stream_openai_compatible` | `except: continue` schluckt Provider-Error-Chunks → User sieht abgeschnittene Antwort ohne Fehler |
| 87 | `backend/app/llm.py:282` `_call_anthropic` | `block["text"]` ohne `.get(...,"")` → KeyError wenn text-Block kein text-Feld hat |
| 88 | `backend/app/rag.py:1117–1119` `rechunk_documents` | Löscht alle chunks vor Re-Embed; Embedding-Provider-Hiccup = Doc bleibt unsearchable mit `error`-Status |
| 89 | `backend/app/main.py:1227–1228` admin grant_admin | bumpt `token_version` nicht (anders als deactivate) — inkonsistent |
| 90 | `backend/app/main.py:1080–1081` `extract_text` Error-Path | Leakt Library-Internals (`PdfReadError`, `BadZipFile`, `xlrd.XLRDError`) zum User |
| 91 | `backend/app/documents.py:309–334` `_split_pdf_into_batches` | Encrypted PDFs werden silent durch Mistral OCR geschickt; User sieht generisches OCR-Failure |
| 92 | `backend/app/documents.py:194` `_extract_xlsx_text` | Formeln aus non-Excel-saves liefern `None` data-only → silent empty-content-Indexing |
| 93 | `frontend/src/components/PoolShareDialog.jsx:75–83` | `expiresAt` nie wirklich wired in UI; `maxUses="0"`-Eingabe unkontrolliert; `navigator.clipboard.writeText` silent-fail → `setCopied(true)` false-positive |
| 94 | `frontend/src/components/AssistantManager.jsx:42, 173` | `is_global`-Feld wird auch bei Non-Admin-Edit im Request mitgesendet — wenn Backend nicht strippt, Privilege-Escalation |
| 95 | `frontend/src/components/MessageInput.jsx:71` | Template-Insertion am Buffer-Ende statt Cursor-Position; kein max_length-Check |
| 96 | `backend/app/admin.py:271–297` `update_rag_settings` | Read-merge-upsert ohne Optimistic-Lock — gleichzeitige Admin-Saves überschreiben einander |
| 97 | `backend/app/pools.py` | Keine Owner-Transfer-Funktion, Owner kann Pool nicht verlassen; Orphan-Owner bei Hard-Delete des Users |

### 🟢 Hinweise (8)

- 98 — Cost-Cap-Lücke: Admin kann `contextual_retrieval_model` auf Sonnet/Opus setzen → N(chunks) × Opus-Calls pro Upload ohne EUR-Cap
- 99 — `update_pool_member` erlaubt Admin Self-Demote zu Viewer → admin-loser Pool bis Owner eingreift
- 100 — `pool_chats.created_by ON DELETE CASCADE` — Hard-Delete eines Users wischt geteilte Pool-Chats (codebase nur soft-deletet, aber manueller Supabase-Studio-Delete würde Daten zerstören)
- 101 — `_apply_contextual_retrieval` `doc_summary = clean_text[:1200]` — sinnlos für lange PDFs (Quality-Bug)
- 102 — `image_storage.mark_image_succeeded` schreibt nicht actual-cost; estimated-cost bleibt im DB-Eintrag
- 103 — `api.js:741` `LLMError`-Dict wird ggf. an User gerendert
- 104 — CSV-Sniffer erkennt `:` oder Leerzeichen nicht
- 105 — `xml_escape` fehlt in weiteren Prompt-Context-Bauer-Stellen (nicht nur `<content>`)

## Verschärfungen aus Runde 4

- **#37 Bilder-Pagination**: Verifier-Split bestätigt — Mathe stimmt im Happy-Path, aber **delete-then-loadmore dupliziert** eine Zeile
- **#51 Chunk_text infinite loop**: gleicher root-cause auch in `_table_to_atoms` (`rag.py:103`)
- **#53 Prompt-Injection**: nicht nur `{content}`, sondern auch `{filename}` und `{section}` unescaped → 3 Injection-Vektoren
- **#55 chat_id PostgREST-Injection**: bare-string-`chat_id`-Pattern an 6+ Stellen in `main.py`, nicht nur 3 in `documents.py`
- **#57 Provider-Error-Leak**: gleicher Anti-Pattern auch in `rag.py:607` für Embedding-API
- **#58 anthropic stop_reason**: gilt für **alle 4 Streaming-Provider**

## Status Master-Liste

- **Stand vor Runde 4:** 75 Findings
- **Stand nach Runde 4:** ~105 Findings (28 kritisch / 8 hoch / 52 worth-fixing / 17 Hinweise)
- **Runde 4 ergab:** 7 neue kritisch, 1 neu hoch, 14 neu worth-fixing, 8 neue Hinweise, plus 6 Verschärfungen
- **DISPUTED:** keine offenen Streitfälle mehr

## Runde-4-Confidence-Statements

Beide Independent-Sweeper sagen unabhängig:
- pools.py jetzt substantiell abgekämmt (~1 mehr)
- admin.py/providers.py 1–2 mehr erwartet
- `_apply_contextual_retrieval` voll abgehandelt
- llm.py Streams: Google-safety-Block-Pfad noch ungetestet (~1–2)
- `assistants.py`/`templates.py` server-side global-flag-Enforcement (~1–2)
- pool_chats.py / pools.py ACL-Pfade (~3–5)
- DOCX/Makro + XLS-legacy-Handling (~1–2)
- TemplatePicker variable-substitution (~0–1)

**Gesamt-Schätzung verbleibend: 8–15 vergleichbarer Schwere.**

---

## Runde 5 — Neue Funde (~15)

### 🔴 Neu kritisch (6)

| # | Datei:Zeile | Was |
|---|---|---|
| 106 | `backend/app/assistants.py:50–65` + `templates.py:43–58` | **Owner-Bypass auf global**: Permission-Check `if a["user_id"] != user_id and not (is_admin and a.get("is_global"))` — Owner-OR-Branch lässt User der Asset einst als Admin erstellt hat auch nach Admin-Demotion weiter schreiben/löschen. Globale Prompts werden von ex-Admins manipulierbar |
| 107 | `backend/app/main.py:1043, 1791, 1917` | `file_bytes = await file.read()` **vor** Size-Check → kein `Content-Length`-Pre-Check, kein Streaming-Limit → OOM-Vektor via riesige Multipart-Uploads |
| 108 | `backend/app/main.py:1241–1258` `admin_delete_user` | Returnt `{"deleted": True}` aber **flippt nur `is_active=False`**. Daten, Chats, Pool-Memberships, owned Pools, Provider-Keys bleiben. GDPR-Auskunfts-/Löschanspruch-Problem |
| 109 | `backend/app/main.py:1672–1693` + Migration | Pool-Owner kann nicht austreten + kein Transfer-Endpoint + `admin_delete_user` deaktiviert nur → **Pool wird un-deletable** wenn Owner deaktiviert wird, kein anderer kann übernehmen |
| 110 | `backend/app/main.py:1326–1340` `admin_rechunk_documents` | `_rechunk_status["state"]` wird **innerhalb** des BackgroundTasks gesetzt; zwischen Response und Task-Start kommt zweiter POST durch → zwei parallele rechunk-Pässe → doppeltes Löschen/Re-Processing |
| 111 | `backend/app/llm.py:307, 554–560` Google | **SAFETY-Block silent**: bei `finishReason="SAFETY"` oder `"RECITATION"` fehlt `content`-Key → `.get("text", "")` resolviert zu `""` → leere Assistant-Message in DB + User zahlt für Prompt-Tokens ohne Antwort |

### 🟡 Worth-fixing (7)

| # | Datei:Zeile | Was |
|---|---|---|
| 112 | `frontend/src/components/PoolHeader.jsx:101` | React `key={member.user_id}` aber `list_members` returnt `id` → alle Keys `undefined`, dedup-Warning, broken-reconciliation |
| 113 | `backend/app/assistants.py` + `templates.py` | **Keine audit_log_event-Aufrufe für CRUD**. Manipulation globaler Prompts (verstärkt #106) ist unsichtbar |
| 114 | `backend/app/documents.py:223` + `backend/uv.lock` | **`xlrd==1.2.0` (2018, unmaintained)** parst attacker-controlled .xls; OLE2/BIFF-Parser hat known crash/infinite-loop-Bugs |
| 115 | `backend/app/assistants.py:59` + `models.py:83–89` | `update_assistant` allowed-Set droppt `is_global` silent — Admin-Demote-Action im UI „speichert" ohne Effekt, dann silent-revert beim Reload. Pydantic ohne `extra='forbid'` |
| 116 | `backend/app/assistants.py:32–36` `list_assistants` | Zwei Queries, dedup nur implizit via `neq("user_id", user_id)` — wenn Self-Toggle zu `is_global=True` jemals an der Route-Schicht vorbei passiert, doppelte Anzeige |
| 117 | `backend/app/storage.py:67–93` `get_conversation` | Kein Ownership-Check in der Datenschicht — relies entirely on caller verify; Defense-in-Depth-Lücke |
| 118 | `backend/app/admin.py:29–41` `update_user` | Keine Self-Demotion-Guard, keine „≥1 aktiver Admin bleibt"-Invariante — Admin kann sich selbst deaktivieren → Platform-Lockout |

### 🟢 Hinweise (5)

- 119 — `pool_chats.created_by` ohne FK auf `app_users(id)` — inkonsistent zu `chats.user_id`-CASCADE
- 120 — Image-LEFT-JOIN-Typ-Ambiguität (`pools.py:469, 487–490`, `storage.py:75, 87–92`): `img_data` kann dict (1:1) oder list (1:N) sein → `.get()` AttributeError-Risiko
- 121 — `_apply_contextual_retrieval` swallows `_generate_chunk_context` exceptions silent → Chunk geht mit leerem Context-Prefix als „erfolgreich" in DB; stille Quality-Korruption ohne Telemetrie
- 122 — JWT-Claim `is_admin` ist tot (`get_current_user` lädt live aus DB) — Dead-Weight, einladend für künftige falsche „Optimierungen"
- 123 — `_inject_system_context` evtl. doppelt injected im Listing-Branch + Chunks-Branch

## Verifikations-Refinements

- **#81 image_url-Scheme**: heruntergestuft auf MEDIUM. React 19 blockt `javascript:`-URI in `<img src>` standardmäßig. Aber `fetch(image_url)` in `Bilder.jsx:147` ist weiterhin SSRF-via-Browser-vulnerable.
- **#58 anthropic stop_reason**: verallgemeinert auf **alle 4 Streaming-Provider** (Azure, OpenAI-compat, Anthropic, Google). Siehe #83 als allgemeinen Eintrag.
- **#1 supabase NameError, #54 XFF, #51 Infinite-Loop, #82 Google-Key**: alle erneut bestätigt durch Round-5-Verifizierer.

## Status Master-Liste

- **Stand vor Runde 5:** ~105 Findings
- **Stand nach Runde 5:** ~123 Findings (34 kritisch / 8 hoch / 59 worth-fixing / 22 Hinweise)
- **Runde 5 ergab:** 6 neue kritisch, 7 neu worth-fixing, 5 neue Hinweise
- **Pattern-Klassen identifiziert:** Read-Modify-Write Atomicity, F-String-XML/SQL-Injection, Sync supabase-py in async, Truthiness vs None, Provider-Error-Body-Leak, Hardcoded i18n, Pydantic-permissive + frontend-trust, Silent-Exception-Swallow in LLM-Layer, Missing-Audit-Coverage for mutations

## Sweep-Sättigungs-Indikatoren

- Patterns wiederholen sich (Runde 5 fand größtenteils Spielarten der bekannten 6 Anti-Pattern-Klassen)
- Confidence-Statements der beiden Independent-Sweeper liegen unter 5 weiteren erwarteten Funden pro Bereich
- IDOR-Verdacht aus Verifier #1 refutiert nach Code-Check (`main.py:2014` enforced)
- **Empfehlung:** Eine letzte fokussierte Runde 6 (engerer Scope) oder Übergang zu Pattern-Fix-Phase

---

## Runde 6 — Neue Funde (~14)

### Verifikations-Refinements zu Runde 5

- **#117 `storage.get_conversation`**: **HERUNTERGESTUFT auf Hinweis** — Route-Layer ruft `verify_conversation_owner` vor jedem Aufruf. Defense-in-Depth-Gap bleibt, heute nicht ausnutzbar.
- **#118 `admin.update_user` self-demotion**: **TEILWEISE HERUNTERGESTUFT** — Route-Layer (`main.py:1216–1220`) blockt Self-Deactivate/Self-De-Admin. **Echte verbleibende Lücke**: „≥1 aktiver Admin bleibt"-Invariante fehlt (Alice de-admin Bob → Bob de-admin Alice → Lockout).
- **#111 Google SAFETY**: verschärft — `_stream_google` konsumiert `usageMetadata.promptTokenCount > 0` aber yieldet keinen Text → `full_content=""` + `record_usage(promptTokens, 0)` als „erfolgreiche leere Antwort" persistiert.
- **#112 PoolHeader Key-Bug**: verschärft — identisches Pattern auch in `PoolOverview.jsx:131`.

### 🔴 Neu kritisch (3)

| # | Datei:Zeile | Was |
|---|---|---|
| 124 | `documents.py:149` `_extract_csv_text` | **Silent CSV-Datenkorruption**: `csv.reader(text.splitlines(), dialect)` — `splitlines()` schneidet *vor* dem CSV-Parser; jedes RFC-4180-zitierte `\n` innerhalb einer Zelle (Adressen, Memos) zerlegt eine Zeile in zwei → Quote-Paarung bricht → Spalten verschieben sich silent in den RAG-Store |
| 125 | `llm.py:_call_anthropic`/`_stream_anthropic` | Silent-Empty bei `stop_reason="tool_use"`/`"thinking"`/`"pause_turn"`/`"refusal"`: Block-Filter dropt non-text → Claude 4.x extended-thinking-only-Response = `text=""`, persistiert als leere Antwort, User wird abgerechnet. Gleiche Pathologie wie #111 |
| 126 | `main.py:1043, 1791` | **Korrektur zu #107**: nur zwei OOM-Vektoren (nicht drei). Line 1917 ist `upload_pool_text` mit bereits geparsten Pydantic-JSON-Body. Zwei Multipart-Pfade bleiben kritisch |

### 🟡 Neu Worth-fixing (10)

| # | Datei:Zeile | Was |
|---|---|---|
| 127 | `admin.py:242–244` `delete_model_config` | Kein `is_default`-Guard — Admin kann einzigen Default löschen → `get_default_model_id` returnt None → Chat-Fallback hängt auf hartkodiertem `DEFAULT_MODEL` |
| 128 | `storage.py:106–112` + `pools.py:495` | `add_user_message`/`add_assistant_message`/`add_pool_chat_message` ohne Idempotenz — Client-Retry nach Timeout → doppelte User-Bubble + zweimal Stream-Run |
| 129 | `main.py:1426` + `providers.py:116` `admin_set_provider_key` | Keine `KNOWN_PROVIDERS`-Validation — `provider="openai-staging"` upsertbar |
| 130 | `rag.py:_apply_contextual_retrieval` | **Uneven Embedding-Space**: `_generate_chunk_context` swallowt Exceptions → manche Chunks bekommen Context-Prefix, andere nicht; alle gehen in selben `embedding`-Column → silent uneven Retrieval-Ranking |
| 131 | `main.py:602` vs `:846` | SSE Client-Disconnect mid-stream: User-Message persistiert, kein Assistant-Write → einseitige Konversation, `is_first_message`-Heuristik bricht |
| 132 | `documents.py:163` `_extract_docx_text` | `doc.iter_inner_content()` walked nur `body` — droppt silent: Headers, Footers, Footnotes, Comments, Text Boxes, SDT Content Controls |
| 133 | `documents.py:161, 194` | **Kein XML/ZIP-Bombe-Schutz** in `.docx`/`.xlsx` — 10 MB komprimiert → potenziell GB inflated (SharedStrings-Bomb klassisch) |
| 134 | `models.py:8,15,23,47,78,88,201,207` | **`temperature` ohne `Field(ge=0, le=2)`-Bounds** in ALLEN Models — Frontend `parseFloat` lässt `NaN`/`Infinity` durch |
| 135 | `models.py:76` `CreateAssistantRequest.system_prompt` | **Keine `max_length`** — 49 KB-System-Prompt speicherbar, wird jeder Chat-Message vorangestellt |
| 136 | `pools.py:118` `update_pool` | **Keine Allowlist** — anders als `update_assistant`/`update_template` die explizite `allowed`-Sets haben; forwarded `**kwargs` zu Supabase |

### 🟢 Neu Hinweise (5)

- 137 — `models.py:166–168` `CreatePoolRequest.description`/`color` unvalidated
- 138 — `MessageInput.jsx:71–76` Template-Variable-Substitution fehlt obwohl `TemplateManager.jsx:136` `{{platzhalter}}`-Syntax bewirbt
- 139 — `PoolDetail.jsx:72–87` Document-Polling-Timer-Leak nach Pool-Switch
- 140 — `providers.py:set_provider_key:116–143` Mid-Stream-Race: in-flight Stream hält alten Key bei Kompromiss-Rotation
- 141 — `pools.py:list_members:155–195` Owner-Row ohne `member_since`, Member-Rows mit — Frontend-NPE-Risiko

### Status nach Runde 6

| Metrik | Vor Runde 6 | Nach Runde 6 |
|---|---|---|
| Total Findings | ~123 | **~141** |
| Kritisch | 34 | **37** |
| Hoch | 8 | 8 |
| Worth-fixing | 59 | **69** |
| Hinweise | 22 | 27 |

### Pattern-Analyse Runde 6

Sehr klar: Runde 6 fand **überwiegend Wiederholungen bekannter Pattern-Klassen** an neuen Stellen.

- P1 (Atomicity): #127, #128, #129
- P5 (Silent-Empty / Provider-Error): #125 (Anthropic-Layer)
- P7 (Pydantic-permissive): #134, #135, #136, #137
- P8 (Silent-Exception-Swallow): #130
- **Neuer Pattern P10**: Defense-in-Depth nur auf Route-Layer (#117, #118 herabgestuft) — Storage/CRUD-Module ohne eigene Guards

**Wirklich neue Befund-Klassen:**
- #124 CSV-Datenkorruption via `splitlines()` — unique
- #132/#133 DOCX/XLSX Content-Loss + ZIP-Bombe — Filetype-Pipeline-Audit-Gap
- #131 SSE-Disconnect-Orphan — Streaming-Lifecycle-Edge

### Sweeper-Confidence nach Runde 6

- 1–2 weitere in `llm.py` Streaming-Error-Edges
- 2–3 weitere in Audit-Completeness-Sweep
- ~1 weitere in `pool_chats.py` retry-Pfad
- ~12 weitere frontend-Komponenten wahrscheinlich gleiche Shape-Bugs (Pool*, Confirm-Dialog-Family, Modal-Family)

**Empfehlung:** Runde 7 würde 5–8 weitere bringen, alle Spielarten bekannter Pattern. **Pattern-Sättigung ist erreicht.** Pattern-Fix-Phase wird produktiver als weitere Sweep-Runden.

---

## Runde 7 — Neue Funde (~22, mehr als erwartet)

Runde 6 schätzte 5–8 für Runde 7 — tatsächlich kamen ~22, weil Frontend-Komponenten-Sweep (12 Komponenten) und Audit-Completeness-Sweep konzentrierte Trefferzonen hatten. Die meisten sind Wiederholungen bekannter Pattern-Klassen (P5, P7, P8, P9, P10), drei sind unique.

### 🔴 Neu kritisch (4)

| # | Datei:Zeile | Was |
|---|---|---|
| 142 | `main.py:1564–1745` + `:2273` | **Pool-Admin-Actions komplett unauditiert**: `create_pool`, `update_pool`, `delete_pool`, `add_pool_member`, `update_pool_member`, `remove_pool_member`, `create_pool_invite`, `revoke_pool_invite`, `join_pool`, `delete_pool_chat` — zero `audit.log_event`. Pool-Admin kann Mitglieder demoten, Invites rotieren, Chats löschen ohne Trail. Verstößt CLAUDE.md:113 |
| 143 | `image_gen.py:154–155, 207–208` + `models.py:221` `ImageGenerationRequest.parameters` | **Parameter-Bypass**: `payload.update(parameters)` ohne Allowlist — User kann `n=10` posten (Daily-Cap rechnet nur 1 Bild), oder `prompt`-Override nach Style-Prefix-Konkatenation, oder anderen Modell-Aufruf trotz teurerem cost_row. Rate-Limit 10/min, also 100 Bilder/Min möglich für Single-Bild-Preis |
| 144 | `frontend/src/App.jsx:736–756` | **Errors silent außerhalb Chat-View**: `error`-State nur in `<ChatArea error={...} />` konsumiert. Alle `setError(e.message)` auf `bilder`/`pools`/`admin`/`pool-detail`/`pool-chat`-Views setzen State, der nie gerendert wird. User sieht hängende Spinner ohne Feedback |
| 145 | `frontend/src/components/ConfirmDialog.jsx:22–29` | **Promise-Leak**: `resolveRef.current` wird auf jedem `confirm()`-Call überschrieben ohne den vorherigen Promise zu resolven. Zwei rasche Confirm-Calls → erster Awaiter hängt **für immer** → `await onDelete()` chains deadlocken |

### 🟡 Neu Worth-fixing (15)

| # | Datei:Zeile | Was |
|---|---|---|
| 146 | `main.py:895–953` + `:964–1020` | Assistants/Templates CRUD unauditiert (extends #113) — manipulierter globaler `system_prompt` ohne Trail |
| 147 | `main.py:1162–1170` | Document-Delete unauditiert |
| 148 | `main.py:341–360` | Conversation-PATCH unauditiert — silent Modell-Re-Pointing möglich |
| 149 | `main.py:2034` | Pool-Chat-Write erlaubt für `viewer`-Rolle — semantisch sollte read-only sein |
| 150 | `main.py:811, 878, 2197, 2264` | `asyncio.create_task(_auto_name_*)` Referenz nicht retained → GC-Risk → silent disappear unter Memory-Pressure |
| 151 | `llm.py` alle Provider | Kein `Retry-After`-Honoring auf 429 — Burst-Traffic kaskadiert zu 502 |
| 152 | `pools.py:268–294` `use_invite_link` | Re-prüft nicht `is_active`/`expires_at` — Race zwischen `get_invite_by_token` (`main.py:1742`) und Increment |
| 153 | `main.py:1173–1189` `rag_search` | Kein Pydantic-Model, kein `query`-Length-Cap — 100 KB-Query → Embedding-Cost-DoS |
| 154 | `frontend/src/api.js:12, 16, 20–22, 25–27` | `localStorage.getItem/setItem` ohne try/catch — Safari-Private-Mode wirft `SecurityError` → App-Crash |
| 155 | `frontend/src/api.js:61–71` `tryRefresh` | Clearet Tokens auf **jeder** non-2xx-Response (auch transienter 502) — User wird ausgeloggt durch Backend-Blip |
| 156 | `frontend/src/components/PoolShareDialog.jsx:33` | `parseInt(maxUses)` ohne Radix — `NaN` möglich; `expires_at` nie im UI exponiert obwohl Backend es akzeptiert |
| 157 | `frontend/src/components/AssistantManager.jsx:96` + `TemplateManager.jsx:91` | Beide rendern eigene `modal-overlay`-Divs **ohne** den `<Modal/>`-Wrapper → kein focus-trap, kein Escape, kein `aria-modal` (Regression von Modal-Audit) |
| 158 | `models.py:75` `CreateAssistantRequest.description` + `UpdateAssistantRequest` | Kein `max_length` — 5 MB-Description möglich |
| 159 | `frontend/src/components/MessageInput.jsx:45–48` | Kein `maxLength={50000}` clientseitig (Backend cappt, aber Auto-Resize bis 240px + 422 ohne UX-Hint) |
| 160 | `backend/app/rag.py:412, 419, 426, 433, 442` `parse_document_filters` | Year-Regex `20[2-9]\d` rejected Jahre <2020 und >2099 → historische Queries kriegen silent kein Date-Filter |
| 161 | `frontend/src/components/SourceDisplay.jsx:6–14` | **Citation-Korrektheits-Bug**: Dedup-Key nur `s.filename` — kollabiert mehrere Pages desselben PDFs → Citation zeigt `(S. 3)` während eigentlicher Treffer auf Page 17. Verstößt CLAUDE.md-Provenance-Regel |
| 162 | `backend/app/storage.py:81–93` `get_conversation` | Selektiert nicht `image_sources` — nach Reload verschwinden Image-Citations während Text-Citations bleiben |
| 163 | `frontend/src/components/PoolChatArea.jsx:23–34` | Keine `temperature`/Template/Upload-Affordances — Pool-Chats können Temperature nicht tunen (Feature-Gap vs personal ChatArea) |
| 164 | `frontend/src/components/Sidebar.jsx:189` + `:281` | Delete-Button auf Chat + Pool ohne `ConfirmDialog` — ein Klick zerstört Konversation |
| 165 | `main.py:341 update_conversation` | PATCH validiert nicht ob `model` in `app_models` existiert — Garbage-Modell-String möglich → `call_llm` 500 später |
| 166 | `frontend/src/components/ChatArea.jsx:47` + `PoolChatArea.jsx:86` | `key={index}` für Messages — Streaming-Content kann in falsche Bubble bleeden nach Retry/Regenerate |
| 167 | `frontend/src/components/MessageBubble.jsx:29, 69–77` | `onGenerateImage`-Prop dead code (kein Caller passt ihn durch) — Recovery-Path bei 404 fehlt |
| 168 | `frontend/src/components/ConfirmDialog.jsx:7–11` | Hartkodierte deutsche Defaults (`Bestätigen?`, `Bestätigen`, `Abbrechen`) — Confirm-Subsystem locale-locked |
| 169 | `main.py:2284` `delete_pool_chat` | Viewer-Rolle darf eigenen Chat löschen — wenn Pool-Admin demote → Member-Write-Lost expected, aber kann eigene shared Chat-History wischen |

### 🟢 Neu Hinweise (8)

- 170 — Keine Background-Cleanup-Jobs in `backend/app/` (`pool_invite_links.expires_at`, `app_generated_images.provider_url_expires_at`, `app_audit_logs`-Retention sind tote Spalten)
- 171 — `_stream_anthropic line[6:]` (`llm.py:504`) brittle — bricht falls Anthropic je `data:` ohne Space sendet
- 172 — `image_gen.py:53, 61–62` UTC-Day-Boundary mismatcht DE-Timezone: Generate um 01:00 Berlin-Zeit zählt gegen vortägiges UTC-Budget
- 173 — `MessageInput.jsx:64–69` Enter-to-send ohne `e.isComposing`-Guard — CJK/Umlaut-Composition kann premature-fire
- 174 — `rag.py:_DOC_TYPE_WORDS` Substring-Match (nicht word-boundary) — "**unprotokollarisch**" matched `protokoll`
- 175 — `Modal.jsx:6` `titleIdCounter` Modul-global-Integer, wächst über SPA-Session-Lebenszeit
- 176 — `PoolDocuments.jsx:65–67` Upload-Error friert Progress-Bar bei letztem `pct` ein
- 177 — `SourceDisplay.jsx:26, 55, 67` hartkodiert englische Labels (`Sources:`, `Image Sources:`, `(p. X)`) — Rest der App ist deutsch

### Verifikations-Refinements zu Runde 6

- **#130 uneven embedding space**: refined auf VERIFIED-WITH-NUANCE — innerhalb eines Single-Upload-Pfads konsistent; Asymmetrie entsteht *über die Zeit* wenn `contextual_retrieval_enabled` getoggelt wird → Mitigation: Rechunk-Endpoint nutzen
- **#136 update_pool no allowlist**: Defense-in-Depth-Lücke confirmed, heute nicht ausnutzbar weil Route-Layer Pydantic-strikt — aber importierbare Funktion ohne Allowlist
- **#112 PoolHeader-Key-Bug**: jetzt wieder unschärfer — Round 7 Verifier sagt `PoolMembers.jsx:95` ist `m.id`-korrekt, nur PoolHeader+PoolOverview falsch

## Status Master-Liste

| Metrik | Vor Runde 7 | Nach Runde 7 |
|---|---|---|
| Total Findings | ~141 | **~163** |
| Kritisch | 37 | **41** |
| Hoch | 8 | 8 |
| Worth-fixing | 70 | **89** |
| Hinweise | 27 | 35 |

### Cumulative Findings pro Runde

| Runde | Neue Findings | Neu kritisch | Neu unique-Pattern |
|---|---|---|---|
| 1 | 15 | 8 | 6 |
| 2 | 13 | 4 | 3 |
| 3 | 25 | 8 | 3 |
| 4 | 25 | 7 | 2 |
| 5 | 18 | 6 | 2 |
| 6 | 14 | 3 | 1 |
| 7 | 22 | 4 | 2 (ConfirmDialog-Reentrancy, params-bypass) |
| **Σ** | **132** | **40** | **19 unique patterns** |

### Pattern-Klassen-Verteilung in Runde 7

| Pattern | Anzahl in Runde 7 |
|---|---|
| P9 Missing-Audit | 4 (Pool-Admin, Assistants/Templates, Doc-Delete, Conversation-PATCH) |
| P7 Pydantic-permissive | 4 (#143 params, #158 description, #159 maxLength, #165 model validation) |
| P5 Provider-Error / Silent-Empty | 2 |
| P3 Sync-supabase-in-async / N+1 | 1 |
| **Unique-Patterns** | 3 (Error-Display-Coverage-Gap #144, ConfirmDialog-Reentrancy #145, Image-Param-Bypass #143) |
| **i18n-Regressions** | 6 hartkodierte deutsche Strings in NavRail/PoolList/Sidebar/PoolChatList/PoolChatArea/ConfirmDialog |

### Confidence-Statements der Round-7-Sweeper

- LLM Streaming-Error-Edges: **saturated**
- Audit-Completeness-Sweep: **saturated** — Pool-Admin-Audit-Gap ist die letzte große Gruppe
- `pool_chats.py`: **saturated** (40 LOC, ausgelutscht)
- `encryption.py`: **saturated** (Fernet-Envelope/Rotation = bekannt)
- `documents.py`: erwarten 2–3 mehr (rechunk/upload-interleave-Races, MIME-Sniffing-Edges nicht voll abgedeckt)
- `image_gen.py` provider-call retry: erwarten 1–2 mehr (_call_provider Cancel-Pfade)
- Frontend (12 Komponenten): **substantially saturated**, ~12 weitere kleinere Stringe-/Form-Drift-Items wahrscheinlich

### Echte Sättigungs-Indikatoren

1. **Keine neuen Pattern-Klassen mehr** seit P10 in Runde 6 — die unique-Findings in Runde 7 (params-bypass, error-display-coverage, ConfirmDialog-reentrancy) sind individual Bugs, keine wiederkehrenden Anti-Patterns
2. **Verifier-Spread konvergiert** — 90%+ Übereinstimmung zwischen informierten und impartial Verifizierern
3. **Audit-Coverage-Map ist jetzt komplett** — alle Mutations-Endpoints klassifiziert in „audited" vs „unaudited"

**Pattern-Fix-Phase ist jetzt überfällig.** Runde 8 würde wahrscheinlich nur noch 3–6 individual Bugs liefern, primär in den noch nicht voll gesweepten Bereichen (`documents.py`, `image_gen.py`, weitere Frontend-Forms).

---

## Runde 8 — Neue Funde (~28, deutlich mehr als erwartet)

Runde 7 schätzte 3–6 für Runde 8 — **tatsächlich kamen ~28 Findings, davon 8 neu kritisch**. Gründe: tieferer Sweep in noch unbeackerten Bereichen (Startup/Lifecycle, SQL-RPC-Schicht, Config-Validation, AuthFetch-Edge-Cases, AdminDashboard-Sub-Tabs) brachte echte neue Schwachstellen ans Licht.

### Verifikations-Refinements zu Runde 7

- **#164 Sidebar delete buttons no ConfirmDialog**: **WRONG** — Confirms leben einen Level höher in `App.jsx:328, 587, 606`-Handlern. Falscher Befund, zurückziehen.
- **#143 image-gen parameters-Bypass**: verschärft — Audit-Log loggt `prompt_length=len(prompt)` des **Originals**, nicht des overrideen Prompts → Audit-Trail blind für den eigentlichen Misuse.
- **#145 ConfirmDialog Promise-Leak**: heruntergestuft auf LOW — passiert in der Praxis nur bei rascher programmatischer Doppel-Call; UI-Modal blockt User-Doppelklick.
- **PoolList.jsx** komplett: **DEAD CODE** — nirgends importiert, abgelöst durch Sidebar.jsx Pool-Branch. Datei + duplicate `pool-join-input-light`-Style können gelöscht werden.

### 🔴 Neu kritisch (8)

| # | Datei:Zeile | Was |
|---|---|---|
| 178 | `backend/app/main.py` | **Kein Admin-Bootstrap on Startup** — zero `@app.on_event`, keine `lifespan`-Handler. CLAUDE.md sagt explizit: *„Admin user is bootstrapped from admin_user/admin_pw env vars at startup"* — kontraktverletzend. **Fresh Deploy hat keinen Admin** bis manuell eine Row geflippt wird. Companion: kein Shutdown-Hook → in-flight rechunk/upload/imagegen werden bei SIGTERM ohne Cleanup gekillt |
| 179 | `backend/app/image_gen.py:392–419` `_call_provider` | **`asyncio.CancelledError` leaks Pending-Row**: in Python 3.8+ erbt CancelledError von BaseException; `except Exception` fängt es nicht → Client-Disconnect mid-OpenAI-Call lässt Row in `status='pending'` für immer. Keine Cleanup-Cron-Job. Wachstum unbounded. Gleicher Bug in `_run_rechunk_task` |
| 180 | `supabase/migrations/20260221_rag_scoped_search.sql:36–53` + `20260222_bm25_fts.sql:60–66` | **Pool-Scope-RAG-RPCs prüfen NICHT pool_members-Membership**: `match_document_chunks`, `match_document_assets`, `keyword_search_chunks` akzeptieren beliebige `match_pool_id`, filtern nur `d.pool_id = match_pool_id`. Nicht `SECURITY DEFINER`. Heute durch App-Layer-`require_pool_role` abgesichert, aber Datenisolations-Boundary liegt im falschen Layer |
| 181 | `backend/app/main.py:1101–1144, 1846–1888` | `process_document` Partial-Failure lässt DB-Row in `processing` für immer — outer `try/except` updated nur in-memory dict, nicht DB-Row-Status. Future-Code-Pfade (Summarize-Exception, Asset-Indexing-Fehler) können Row für immer in `processing` lassen → `find_existing_document_by_hash` (`status='ready'`-Filter) entdeckt sie nicht für Dedup |
| 182 | `backend/app/models.py:141–146` `UpdateModelConfigRequest` + `admin.py:210` allowed-Set | **`pricing` silent gedroppt** bei PATCH — Field nicht in Model, nicht in allowed-Set; Pydantic `extra='ignore'` Default-Verhalten dropt es silent → API-Contract gibt 200 zurück, Row unverändert. Wartet auf kommendes UI das daran scheitert |
| 183 | `backend/app/models.py:57–59` `LoginRequest` | **Keine `min_length`/`max_length`** — anders als `RegisterRequest`. 100 MB-Passwort-POST passiert Pydantic, wird Bcrypt-truncated, aber Body parsed + Audit-Row geschrieben. DoS-Amplifier kombiniert mit `failed-login`-Audit-Bloat |
| 184 | `backend/app/config.py:51–52` | **`JWT_SECRET` akzeptiert jeden non-empty Wert** — `JWT_SECRET="x"` bootet fine; HMAC-SHA256 mit 1-Byte-Secret in Sekunden brute-forcebar. Muss `len >= 32` enforced sein |
| 185 | `backend/app/main.py:2194, 2243` `add_pool_chat_message` (assistant turn) | **`user_id` nicht propagiert für Assistant-Turn** — User-Message wird mit `user_id=current_user["id"]` geschrieben (`:2051`), Assistant-Turn aber ohne. In shared Pool-Chat mit mehreren Viewern → Assistant-Turn unattribuiert → Audit/Forensik kann LLM-Spend-Auslöser nicht rekonstruieren |

### 🟡 Neu Worth-fixing (15)

| # | Datei:Zeile | Was |
|---|---|---|
| 186 | `frontend/src/main.jsx` + alle Komponenten | **Keine ErrorBoundary anywhere** — Zero `componentDidCatch`/`getDerivedStateFromError`. Jeder Throw → White-Screen ganze SPA |
| 187 | `supabase/migrations/20260216_phase_d_admin_audit.sql:35` vs `main.py:1338, 1447, 1463` | `audit.target_id UUID`-Column, aber Code passt String-Literals `"all"`, `"openai"` rein → Postgres weigert Cast → `audit.log_event` try/except swallowed → **Rechunk-Start, Provider-Key-Set/Delete-Audits gedroppt silent** |
| 188 | `main.py:1545–1558` `/api/admin/audit-logs` | Akzeptiert negativen `limit`, negativen `offset` — `limit=0` → `.range(0,-1)` = Postgres returnt alle Rows; `limit=-1` → ungewolltes Verhalten |
| 189 | `main.py:1545` `/api/admin/audit-logs` | Endpoint selbst nicht audit-loggt — Admin kann andere User-Aktivität (IPs, Modell-Wahl, File-Hashes, Pool/Chat-IDs) lesen ohne Trail. Surveillance möglich |
| 190 | `admin.py:271–297` `update_rag_settings` | Read-Merge-Upsert ohne Optimistic-Lock — zwei gleichzeitige Admin-PATCHes verlieren silent eine Set Changes |
| 191 | `main.py:1071, 1818` | **Dedup-Audit-Metadata leakt `content_hash`** — Admin mit Audit-Lese-Zugriff kann verifizieren ob spezifische Datei jemals von irgendwem upgeloadet wurde → defeats Pro-User-Document-Confidentiality |
| 192 | `rag.py:1117–1119` | `rechunk_all_documents` vs concurrent Upload-Race: rechunk löscht Chunks + flippt `status='processing'`. Concurrent Upload mit gleichem `content_hash` misst Dedup (`status='ready'`-Filter) → Duplikat-Document mit gleichem Hash erzeugt |
| 193 | `rag.py:1117–1125` `_run_rechunk_task` | Per-Doc-Failure orphan: wenn `delete()` succeedet aber `process_document` raised vor `update_document_status('error')` → Document hat zero Chunks AND `status='processing'`. `except Exception` fängt nicht CancelledError |
| 194 | `frontend/src/components/AdminDashboard.jsx:1673–1675` AuditTab | useEffect ohne Debounce — jeder Keystroke im Filter fired Fetch; backend `.eq("action", filter)` returnt fast immer `[]` bis voller String getippt |
| 195 | `AdminDashboard.jsx:1682` AuditTab | Backend `list_audit_logs` supportet `user_id_filter`; UI hat keine Input dafür — third Param `null` hardcoded. Dead Capability |
| 196 | `AdminDashboard.jsx:1668–1696` AuditTab | Pagination `hasMore = data.length === LIMIT` → Off-by-one wenn exakt LIMIT Rows zurückkommen |
| 197 | `frontend/src/api.js:35–49` `authFetch` | **Concurrent-Refresh-Storms self-throttle**: 5 parallele Fetches alle hitten 401 → 5 parallele `tryRefresh()`-Calls counten gegen `/api/auth/refresh` 30/minute → Storm bei App-Wake 429't sich selbst |
| 198 | `App.jsx:78–83` | **Kein Multi-Tab-Sync** — keine `storage`-Event-Listener; Logout in Tab A lässt Tab B logged-in bis Reload. Kein `visibilitychange` für Re-Auth nach Idle |
| 199 | `main.py:1426` + `providers.py:116` `admin_set_provider_key` | Akzeptiert beliebige `provider`-Namen (überlappt #129 — bestätigt) |
| 200 | `frontend/src/api.js:998–1042, 712–756` Streaming-SSE | Keine `AbortController`, keine Cancellation — Unmount mid-stream → `onDelta`/`onDone` auf stale Closures, React-Warnings, **Provider-Billing läuft weiter für Tokens die User nicht mehr sieht** |
| 201 | `main.py:1523` Google-Provider-List | **API-Key in URL** (`?key={api_key}`) — same Anti-Pattern wie #82, anderer Endpoint, verifiziert |

### 🟢 Neu Hinweise (5)

- 202 — `embedding_provider`/`embedding_deployment` (`models.py:155–156`) keine Enum/Length-Validation → Typo bricked RAG
- 203 — `/api/models` (`main.py:197`) unauthenticated → leakt Tenant-Modell-Config zu anonymen Callern
- 204 — `MAX_UPLOAD_SIZE_MB` (`config.py:44`) keine Upper-Bound — Admin kann 100000 setzen
- 205 — Migration-Overload-Risiko: `match_document_chunks` und `match_document_assets` je 3× `CREATE OR REPLACE` in verschiedenen Migrations. Wenn `_drop_old_function_overloads` selectiv übersprungen wird → „function is not unique"-Errors zur Laufzeit
- 206 — `BildmodelleTab` `parseFloat(payload.cost_per_image)` ohne NaN-Guard → NaN serialisiert als `null` aber `api.adminCreateImageModel` checked `!== null` → Form submitted broken Pricing-Record

## Status Master-Liste

| Metrik | Vor Runde 8 | Nach Runde 8 |
|---|---|---|
| Total Findings | ~163 | **~191** |
| Kritisch | 41 | **49** |
| Hoch | 8 | 8 |
| Worth-fixing | 89 | **104** |
| Hinweise | 35 | 40 |

### Cumulative

| Runde | Neue Findings | Neu kritisch |
|---|---|---|
| 1 | 15 | 8 |
| 2 | 13 | 4 |
| 3 | 25 | 8 |
| 4 | 25 | 7 |
| 5 | 18 | 6 |
| 6 | 14 | 3 |
| 7 | 22 | 4 |
| **8** | **28** | **8** |
| **Σ** | **160** | **48** |

### Sättigungs-Pattern: nochmal überrasch

Runde 8 fand **mehr** kritische Findings als Runde 7 — entgegen Sättigungs-Erwartung. Ursache: zwei „blinde Flecken" wurden geöffnet:

1. **Lifecycle/Startup-Bereich** war nie systematisch geprüft — #178 (kein Admin-Bootstrap), #179 (CancelledError), #181 (process_document Partial-Failure), #193 (rechunk-CancelledError) sind alle Lifecycle-Issues
2. **SQL-RPC-Layer** war nie verifiziert — #180 (Pool-RPCs prüfen Membership nicht), #205 (Migration-Overload-Risiko)

Diese zwei Bereiche liefern wahrscheinlich noch 5–10 weitere Findings, wenn man sie systematisch durchgeht. Dann ist die Sättigung wirklich erreicht.

**Echte Pattern-Klassen-Sättigung:** Keine wirklich neuen Pattern-Klassen entdeckt (P1–P10 decken alles ab). Aber **konkrete Trefferzonen sind noch nicht erschöpft**.

### Empfehlung

Runde 9 würde sinnvoll sein **fokussiert auf**:
- Lifecycle: `@app.on_event`/lifespan, Shutdown-Hooks, Admin-Bootstrap-Race
- SQL-RPC-Layer: jede Migration mit `CREATE OR REPLACE FUNCTION` durchgehen, Membership-Enforcement prüfen
- AuthFetch-Refresh-Race-Mutex
- Multi-Tab-Sync-Patterns

Wahrscheinlicher Ertrag Runde 9: ~10–15 weitere Findings, dann **endgültige Sättigung**.

---

## Runde 9 — Neue Funde (~20)

Runde 8 schätzte 10–15. **Tatsächlich kamen ~20, davon 8 neu kritisch.** Frontend-Auth-Layer wurde nie vorher systematisch geprüft → reiche Trefferzone.

### 🔴 Neu kritisch (8)

| # | Datei:Zeile | Was |
|---|---|---|
| 207 | `frontend/src/auth.jsx` (existiert nicht) + `App.jsx` + `api.js` | **Kein AuthContext existiert** — entgegen Workspace-CLAUDE.md (`auth.jsx`/AuthContext-Pattern). Auth-State lebt in **zwei disconnected places**: `localStorage` (in `api.js`) und `useState` in `App.jsx:19`. Children prop-drillen User. Keine zentrale Token-Lifecycle-Koordination |
| 208 | `frontend/src/api.js:68–71` `tryRefresh` | **Clearet Tokens auf JEDER non-2xx Response** — bare `catch { clearTokens(); return false }`. Ein transienter 502/503 von `/refresh` loggt User komplett aus. Kein Retry, keine Unterscheidung zwischen „Netz down" und „Refresh-Token revoked" |
| 209 | `frontend/src/api.js:998–1042, 712–756` Streaming-SSE | **Long-stream + Access-Token-Ablauf**: SSE-Stream nutzt initial-issued Token. Bei 30-min-Image-Gen oder Long-Completion läuft Token ab; Stream **kann nicht mid-stream refresht werden** → eventuell 401 oder hängt. Mid-stream-Refresh-Pattern existiert nicht |
| 210 | Frontend gesamt | **Kein `token_version`-Check** — Backend bumpt `token_version` bei `bump_token_version` (`auth.py:171`), aber Frontend liest/vergleicht es nie. Tab das nie 401 sieht (z. B. lang offen, nur Reads) **behält seinen Access-Token bis Ablauf** trotz DB-`token_version`-Bump → Admin-Rotation funktioniert nicht für offene Tabs |
| 211 | `backend/app/auth.py:171–177` `bump_token_version` | **Read-Modify-Write Race**: liest current (L173), incrementiert in Python (L176), schreibt zurück (L177). Zwei parallele Admin-Demotions/Password-Resets: beide lesen N, beide schreiben N+1 → tatsächlicher Advance +1 statt +2 → Token mit `version=N+1` der zwischen den Bumps issued wurde überlebt fälschlicherweise den zweiten Bump |
| 212 | `backend/app/auth.py:142–150` `authenticate_user` | **Username-Enumeration Timing-Oracle**: returnt sofort bei `result.data` empty (L145), läuft volle bcrypt (~100ms) wenn User existiert. Attacker enumeriert valide Usernames via Response-Timing. Companion: failed-login-Audit mit username verstärkt Channel |
| 213 | `backend/app/config.py:10` + `main.py:175–181` | **CORS-Default unsafe mit `allow_credentials=True`**: Default-CORS-Origins `"http://localhost:5173,..."` wenn Env fehlt in PROD-Deploy. Kombiniert mit `allow_credentials=True` (L178) → misconfigured Deploy akzeptiert credentialed Requests von beliebigem localhost-Service den User laufen hat |
| 214 | `backend/app/main.py:1299, 1326–1340` `_rechunk_status` | Module-Global-Dict ist **process-local**. Zwei uvicorn-Worker haben unabhängige Dicts. Worker A startet rechunk; Worker B's `/rechunk-status` returnt `idle`; Worker B's `admin_rechunk_documents` passt auch den `running`-Guard und startet **zweiten parallelen Rechunk**. Multi-Worker-Konsistenz gebrochen, gleicher Pattern wie `RATE_LIMIT_STORAGE_URL="memory://"` |

### 🟡 Neu Worth-fixing (10)

| # | Datei:Zeile | Was |
|---|---|---|
| 215 | `frontend/src/api.js:19–22` `setTokens` | localStorage-Quota-Exceeded nicht gehandelt — Safari Private Mode + Quota-Exceeded werfen `QuotaExceededError`; Login appeart erfolgreich (Response geparsed) aber Tokens verloren, next `getMe()` returnt null, User silent bounced zu Login |
| 216 | `frontend/src/App.jsx:241–253` `handleLogout` | Keine `window.location.reload()` + kein `pageshow`-Handler mit `event.persisted` → Browser-back nach Logout zeigt bfcache-restored UI bis nächster 401 |
| 217 | `frontend/src/App.jsx:111–149` Login-Concurrency-Race | Sechs `useEffect` Data-Loader (`usage`, `conversations`, `assistants`, `templates`, `pools`, `models`) fired parallel gegen frisch issued Token nach Login. Keine Coordination, kein Loading-Skeleton — User sieht Shell mit Listen die piecewise reinpoppen. Wenn Token kurz ungültig → 6 parallele 401 → 6 parallele Refresh-Calls (siehe #197) |
| 218 | `backend/app/config.py:33` `int(env)` | Crash bei Import-Zeit auf malformed env (z. B. `EMBEDDING_DIMENSIONS=`); kein graceful fallback. Coolify-Typo → Container-Crashloop |
| 219 | Migration-Layer | **`SECURITY DEFINER + SET search_path` fehlt auf ALLEN PL/pgSQL-Funktionen** — heute durch `service_role`-Only-Access mitigiert (anon/authenticated revoked), aber Defense-in-Depth-Lücke. Future Grant `EXECUTE` auf `authenticated` → search_path-Injection-Surface |
| 220 | `backend/app/main.py:1241–1258` `admin_delete_user` | **Returnt `{"deleted": True}` aber ist Soft-Delete** (#108 reaffirmed). Companion: Future-Code könnte `ON DELETE CASCADE` von `app_users` erwarten der nie feuert |
| 221 | `backend/app/audit.py:46–62` `log_event` | Synchroner DB-Write im async-Pfad — jedes audit-loggte Endpoint blockt Event-Loop für Supabase-Round-Trip. Bei N audit_log_events pro Request kaskadiert |
| 222 | `backend/app/main.py:248–270` `/auth/refresh` | **Kein Refresh-Token-Rotation** + kein `token_version`-Bump → Refresh-Token ist 7-Tage-Bearer-Credential. Bestätigung #20 + Verschärfung: kombiniert mit #197 (No-Dedup) Refresh-Storm |
| 223 | Migration `20260513_a_image_generation.sql:24` | `app_generated_images.chat_id REFERENCES chats(id)` — `chats`-Tabelle nicht im `supabase/migrations/`-Tree definiert (nur `pool_chats`); legacy-schema-Annahme. Fresh-Deploy könnte FK-Constraint scheitern |
| 224 | `backend/app/pools.py:495–514` | `add_pool_chat_message` validiert nicht `role` — DB-CHECK fängt es, Caller bekommt 500 statt 400 |

### 🟢 Neu Hinweise (5)

- 225 — Audit-Retention: kein Partitioning, kein TTL, kein Admin-Purge-Endpoint (`20260216_phase_d_admin_audit.sql:30–44`)
- 226 — Image-Cost nicht refunded bei async Cancel — `cost_usd`-Audit (`image_gen.py:385`) loggt vor Provider-Call; Cancelled Image zählt trotzdem zum User-Cost-Ledger
- 227 — `_run_rechunk_task` Per-Doc-Failure: chunks bereits gelöscht, `process_document` raised vor `update_document_status('error')` → Document hat zero chunks + status='processing'
- 228 — Migration-Overload-Risiko: `match_document_chunks` **vier-fach** redefiniert (`20260216_phase_c_rag.sql:40`, `20260218_pools.sql:81`, `20260221_rag_scoped_search.sql:16`, `20260223_chunk_page_number.sql:8`). `20260219_drop_old_function_overloads.sql` existiert aber enforced keine Order
- 229 — `pool_chat_messages.content NOT NULL DEFAULT ''` (`20260218_pools.sql:68`) → empty Assistant-Turns insertable, relevant zu #131 SSE-Disconnect

## Status Master-Liste

| Metrik | Vor Runde 9 | Nach Runde 9 |
|---|---|---|
| Total Findings | ~191 | **~211** |
| Kritisch | 49 | **57** |
| Hoch | 8 | 8 |
| Worth-fixing | 104 | **114** |
| Hinweise | 40 | 45 |

### Cumulative

| Runde | Gefunden | Neu kritisch | Erwartet | Über/Unter |
|---|---|---|---|---|
| 1 | 15 | 8 | — | — |
| 2 | 13 | 4 | — | — |
| 3 | 25 | 8 | 8–15 | + |
| 4 | 25 | 7 | 8–15 | + |
| 5 | 18 | 6 | 8–15 | im Rahmen |
| 6 | 14 | 3 | 8–15 | im Rahmen |
| 7 | 22 | 4 | 5–8 | **über** |
| 8 | 28 | 8 | 3–6 | **massiv über** |
| **9** | **20** | **8** | **10–15** | **leicht über** |

### Sättigungs-Analyse nach Runde 9

**Drei Vorhersagen in Folge unterschätzt:** R7 (5–8 → 22), R8 (3–6 → 28), R9 (10–15 → 20). Die Sweeper sind systematisch zu konservativ.

**Aber:** Die Trefferzonen werden kleiner. R9-Funde sind fast alle in **zwei Bereichen**:
1. Frontend-Auth-Layer (kein AuthContext, Token-Lifecycle-Coordination) → erschöpft nach R9
2. Lifecycle-Race-Conditions (rechunk multi-worker, bump_token_version, login-storm) → fast erschöpft

**Echte Pattern-Klassen-Verteilung in Runde 9:**

| Pattern | R9-Funde |
|---|---|
| P1 Atomicity | #211, #214 |
| P5 Silent Error Swallow | #208 |
| P9 Missing-Audit | (überlappt) |
| P10 Defense-in-Depth Route-Only | #213, #219 |
| **Neu P11**: Frontend-Auth-Coordination | #207, #208, #209, #210, #215, #216, #217 |

**P11 ist die neue Pattern-Klasse**: alle Frontend-Auth-Lifecycle-Mängel sind eine kohärente Familie — kein AuthContext, kein Refresh-Dedup, kein Multi-Tab-Sync, kein `token_version`-Check, kein bfcache-Handling. Lösung: AuthContext mit Token-Lifecycle-Coordinator als architekturelle Einheit.

### Empfehlung

Runde 10 würde sinnvoll sein **fokussiert auf**:
- `frontend/src/api.js` weiterhin (Streaming-Auth-Edges, mid-stream-Token-Refresh)
- Migration-RPCs systematisch durchgehen (jede `CREATE OR REPLACE FUNCTION`)
- `backend/app/storage.py` und `pool_chats.py` Cascading-Edges
- Backend-Module noch nicht voll abgedeckt: `image_storage.py`, `pool_chats.py`, `assistants.py`, `templates.py`

**Wahrscheinlicher Ertrag Runde 10: ~10–18 weitere Findings** (Sweeper-Vorhersagen weiter +50% kalibrieren). Pattern-Klassen sind erschöpft — alle neuen Funde werden Variationen von P1–P11.

---

## Runde 10 — Neue Funde (~23)

Runde 9 schätzte 10–18 (mit Kalibrierung 15–25). **Tatsächlich kamen ~23, davon 6 neu kritisch** — perfekt im Korridor. Erstmals trifft die Vorhersage.

### 🔴 Neu kritisch (6)

| # | Datei:Zeile | Was |
|---|---|---|
| 230 | `backend/app/image_gen.py:203, 254-259` | **xAI-Image-Gen komplett broken** — Provider-Name-Mismatch: Code dispatched auf `provider == "xai"` (ohne Hyphen) aber Rest der Codebase nutzt durchweg `"x-ai"` (mit Hyphen) inkl. `providers.py:13`, `llm.py:40`, `app_model_config.provider`. Image-Request mit xAI-Modell → `HTTPException 400 "Unsupported image provider: x-ai. Supported: openai, xai"`. **xAI-Image-Gen ist tot.** |
| 231 | `supabase/migrations/20260219_phase_f_multimodal_assets.sql:28` + `20260221_rag_scoped_search.sql:103` | **`match_document_assets` PGRST203-Ambiguität live** — `20260219_drop_old_function_overloads.sql:14` droppt die 5-arg-Form **bevor** sie in `phase_f` erstellt wird (alphabetische Sortierung). Dann 6-arg-Form in `20260221` → beide Signaturen koexistieren. PostgREST-Resolution-Fehler bei 5-Default-eligible Calls |
| 232 | `backend/app/main.py:2008–2050, 2273–2286` + `pools.py:459` `get_pool_chat` | **Cross-Pool-Chat-Leak via Route-Mismatch**: Route enforced `require_pool_role(pool_id, …)` aber `get_pool_chat(chat_id)` returnt Chat ohne `chat["pool_id"] == route_pool_id`-Check. Member von Pool A kann `/api/pools/A/chats/<chat_in_pool_B>` aufrufen → bekommt Pool-B-Chat zurück. Gleicher Bug auf POST/DELETE-Routes |
| 233 | `backend/app/assistants.py:64` + `templates.py:57` | Admin-Edit eines globalen Assets schreibt **wrong `user_id` ownership** — Update geht durch, aber `user_id`-Spalte bleibt original Creator. Kombination mit `list_assistants` Own-Branch (`eq("user_id", user_id)`) und Global-Branch (`neq("user_id", user_id)`) → **Original-Owner sieht globales Asset doppelt-rendered** in eigener und globaler Liste |
| 234 | `frontend/src/api.js:712–756, 998–1042` Streaming-SSE | **Stream-Abort verliert partial assistant-content + Token-Usage** — wenn `stream_llm` mid-stream raised, gestreamte `full_content` wird **discarded** (kein `storage.add_assistant_message`-Call im Error-Branch). User-Message bei Z. 602 bereits committed → DB hat User-Turn ohne Assistant-Turn. Next-Message replays als hätte Modell nie geantwortet. Auch: `record_usage` nicht invoked → partial-stream tokens unbilled. Bestätigt #10 mit Mid-Round-9-Verschärfung |
| 235 | `backend/app/llm.py:296, 532` + Error-Path | **Google API-Key leakt via Error-Body**: URL embeddet `?key={api_key}` (#82). Bei non-200 wird `body.decode()[:500]` in `LLMError` geshoved → echo'd Request-URL inkl. Key in Exception → reicht User via SSE `data: {"error": str(e)}` (`main.py:881`). Mehr als nur Log-Leak, **User-sichtbar** |

### 🟡 Neu Worth-fixing (12)

| # | Datei:Zeile | Was |
|---|---|---|
| 236 | `backend/app/admin.py:242–244` `delete_model_config` | Kein `is_default`-Guard — Admin löscht das einzige Default-Model → `get_default_model_id` returnt None → Platform-wide Chat-Failure (verschärft #127) |
| 237 | `backend/app/admin.py:266` `update_rag_settings` | Akzeptiert `embedding_provider="azure"` ohne `embedding_deployment`-Pflichtprüfung → next embed-call kracht zur Laufzeit |
| 238 | `frontend/src/api.js:129–146` `login`/`register` | Bypassen `extractDetail` (`data.detail || ...`) — Pydantic-RequestValidationError ist Array von Error-Objekten → `||` ist truthy → UI rendert `[object Object]` |
| 239 | `frontend/src/components/AdminDashboard.jsx:533–542` RetrievalTab | Pollt `/admin/rechunk-status` mit **1 Hz indefinitely**, kein Backoff. Rechunk auf großem Corpus = 600+ Admin-Requests → Performance-Degradation für alle |
| 240 | `backend/app/main.py:811, 878, 2197, 2264` | `asyncio.create_task` **ohne Exception-Handler attached + ohne Referenz held** — Exception schlucked silent, GC kann mid-flight killen (#150 reaffirmed) |
| 241 | `supabase/migrations/20260216_phase_c_rag.sql:15` | `app_documents.status` **keine CHECK-Constraint** — Werte `processing|ready|error` sind Konvention; Typo silent landed; `match_document_chunks d.status = 'ready'` versteckt dann Docs |
| 242 | `backend/app/documents.py:944` `update_document_status` | Reset auf `chunk_count=0` bei jedem Error-Update (Default-Value-Bug) → observed Counter-Loss bei rechunk-Fail |
| 243 | `supabase/migrations/20260222_bm25_fts.sql:11` | `STORED` generated `content_fts` **locale-locked auf `german`** — i18n-RAG-Wechsel = Column-Drop + GIN-Rebuild = Table-Rewrite |
| 244 | `supabase/migrations/20260216_phase_c_assistants_templates.sql:7,13` | `assistants.is_global=TRUE`-Rows attached an Single-`user_id`-FK mit `ON DELETE CASCADE` → User-Delete killt Global für alle (überlappt #237) |
| 245 | Migration-Layer | **Keine Trigger-Functions** — `pool_pools.updated_at`, `app_generated_images.updated_at` etc. haben Default aber **never advance** beim Update |
| 246 | `backend/app/image_storage.py:79–102` `mark_image_succeeded`/`mark_image_failed` | **Keine State-Transition-Guards** — Update by id only, kein `.eq('status','pending')` → `succeeded → failed` oder Replay möglich. Auch: `result.data[0]` ohne Guard → IndexError-Risk |
| 247 | `backend/app/llm.py` alle Provider | **Kein Retry/Backoff anywhere** — kein `Retry-After`-Honoring, kein Exponential-Backoff auf 429/5xx, Timeout-Inkonsistenz: Non-Stream 60s, Stream 120s, Image-Gen 30s. Single-Hiccup → User-facing 502 ohne Retry |

### 🟢 Neu Hinweise (5)

- 248 — `backend/app/providers.py:237` `test_provider` für Azure schickt **echte Completion** → bezahlt für Konnektivitäts-Check. Sollte `/openai/models` listing nutzen
- 249 — `backend/app/admin.py:48–52` end-date filter off-by-one: `+"T23:59:59"` misst letzte Sekunde + ignoriert User-Timezone
- 250 — `frontend/src/api.js:7` `extractDetail` liest nur `[0].msg` — Multi-Field-Validation-Errors zeigen nur ersten
- 251 — `backend/app/image_storage.py:22` `resolve_image_url` silent fallback auf bare `image_url` bei unknown `storage_kind` — future Migration mit signed-URL-Need scheitert silent
- 252 — `frontend/src/components/AdminDashboard.jsx` viele Inline-Styles → blockt permanent `style-src 'self'` CSP-Hardening (kein User-Input-Flow heute, aber Pattern-Lock)

## Status Master-Liste

| Metrik | Vor R10 | Nach R10 |
|---|---|---|
| Total Findings | ~211 | **~234** |
| Kritisch | 57 | **63** |
| Hoch | 8 | 8 |
| Worth-fixing | 114 | **126** |
| Hinweise | 45 | 50 |

### Cumulative

| Runde | Gefunden | Neu kritisch | Erwartet | Trefferquote |
|---|---|---|---|---|
| 1–6 | 110 | 36 | — | — |
| 7 | 22 | 4 | 5–8 | über |
| 8 | 28 | 8 | 3–6 | weit über |
| 9 | 20 | 8 | 10–15 (kalibriert 15–25) | im Rahmen |
| **10** | **23** | **6** | **10–18 (kal. 15–25)** | **perfekt im Korridor** |

### Sättigungs-Analyse: Vorhersage stimmt jetzt

R10 ist die erste Runde wo der kalibrierte Korridor (15–25) genau getroffen wurde. Das deutet darauf hin, dass:

1. **Sweeper-Konservatismus ist erfolgreich kalibriert** (+50% Aufschlag auf Konfidenz-Statements)
2. **Trefferzonen-Erschließung** ist abgeschlossen — keine neuen blinden Flecken mehr
3. **Echte Findings-Rate** liegt bei ~20 pro Runde, konzentriert auf bekannte Pattern-Klassen P1–P11

### Pattern-Verteilung in Runde 10

| Pattern | R10-Funde |
|---|---|
| P1 Atomicity | #246 (Image-State-Machine) |
| P5 Silent-Error / Body-Leak | #235 (Google-Key-via-Error), #251 (resolve_image_url) |
| P7 Pydantic-permissive | (kein neuer) |
| P8 Silent-Exception-Swallow | #234 (Stream-Abort), #240 (create_task) |
| P9 Missing-Audit | (überlappt mit früheren) |
| P10 Defense-in-Depth Route-Only | #232 (Cross-Pool-Chat) |
| P11 Frontend-Auth | (erschöpft in R9) |
| **Unique** | #230 xAI-Provider-Name, #231 PGRST203-Overload, #233 Wrong-user_id, #245 Trigger-Functions absent |

Vier wirklich unique-Funde — ähnlich R7. Pattern-Klassen sind erschöpft aber konkrete Stellen werden weiter gefunden.

### Empfehlung für Runde 11

Wahrscheinlicher Ertrag: **15–25 weitere Findings**, davon evtl. 3–5 neu kritisch. Fokus-Bereiche werden nur noch:

- Provider-Name-Konsistenz-Audit (nach #230 wahrscheinlich weitere)
- Migration-RPC-Reste (Trigger-Functions, generierte Spalten)
- Frontend-Komponenten-Restkomponenten
- Cross-File-Konsistenz (FK-Cascades vs Code-Assumptions)

**Realistische Erschöpfung:** Runde 12 oder 13 wird wahrscheinlich auf 5–10 Findings runtergehen.

---

## Runde 11 — Neue Funde + Verifikations-Korrekturen

**Wichtigste Erkenntnis: drei Round-10-Funde wurden refutiert.** Bisher noch nicht in einer Sweep-Runde so viele False-Positives auf einmal — Indikator dass die produktive Trefferzone schrumpft.

### Verifikations-Korrekturen (Refutierungen)

| # | Status | Begründung |
|---|---|---|
| **#231 match_document_assets PGRST203** | **REFUTIERT** | Beide Funktionen haben identische 6-arg-Signatur; `CREATE OR REPLACE` ersetzt sauber. Drop-Migration `20260219_drop_old_function_overloads.sql:14` handhabt die 5-arg-Legacy korrekt. **Keine Live-Ambiguität.** |
| **#232 Cross-Pool-Chat-IDOR** | **REFUTIERT** | `main.py:2016, 2037, 2281` enforce alle drei `chat["pool_id"] != pool_id` 404-Check VOR Rückgabe. Round-10-Verifizierer hatte falsche Code-Stelle gelesen. |
| **#233 Admin-Edit Global wrong user_id** | **REFUTIERT** | `allowed`-Set in `assistants.py:59` und `templates.py:52` excludet `user_id`. List-Query nutzt `.neq("user_id", user_id)` → keine Doppel-Render. |

Drei „kritische" R10-Findings entfernen → effektive Total-Korrektur: **−3 von kritisch**.

### 🔴 Neu kritisch (3)

| # | Datei:Zeile | Was |
|---|---|---|
| 253 | `backend/app/rag.py:412–422` `parse_document_filters` | **Day-Level wird silent verworfen**: Regex `\b(\d{1,2})\.(\d{1,2})\.(20[2-9]\d)\b` captured 3 Gruppen aber Code nutzt nur month + year. User-Query „Rechnung vom 23.03.2026" → kompletter März 2026 zurück. Identisch in ISO-Pfad Z. 419. Verletzt CODING-DOKUMENT-Provenance-Versprechen |
| 254 | `backend/app/rag.py:1337–1339` `build_rag_context` | **`continue` statt `break` bei Token-Budget-Overflow** — Chunks kommen sortiert nach Relevanz, top-Chunk übersteigt Budget → Loop scant weiter und emittiert **kleinere, niedrig-relevante** Chunks → silent Rank-Inversion. Plus: `_tok(doc_block)` zählt XML-Wrapper (~10-15 Token/Block) während `chunk.token_count` in DB nicht matcht → ~5-10% Budget-Skew |
| 255 | `backend/app/main.py:783, 2192` Non-Stream-Pfade | **Provider-Error-Bodies leaken auch über Non-Stream-Endpunkte**: `LLMError` mit `resp.text[:500]` (alle 8 Provider-Calls in `llm.py`) wird re-raised als `HTTPException(detail=str(e))`. Companion zu #235 für Non-Stream-Pfade — Round 10 hatte nur den Stream-Pfad geprüft |

### 🟡 Neu Worth-fixing (8)

| # | Datei:Zeile | Was |
|---|---|---|
| 256 | `supabase/migrations/20260513_a_image_generation.sql:55–57` | **`app_image_style_presets`-Global-Uniqueness nicht erzwingbar** — Partial Unique Index `(scope_type, scope_id) WHERE is_active = true` failt für `scope_type='global'` weil `scope_id IS NULL` und Postgres NULL-distinct. Mehrere aktive Globals koexistierbar. Fix: `NULLS NOT DISTINCT` (PG ≥15) oder COALESCE-Sentinel |
| 257 | `supabase/migrations/20260513_a_image_generation.sql:46` | `app_image_style_presets.scope_id` **keine FK** — `scope_type='user'/'pool'/'team'` mit unconstrained UUID. Deleted Pool/User hinterlässt strandierte Preset-Rows |
| 258 | `backend/app/pools.py:259–260` | Invite-Expiry-Parsing: `datetime.fromisoformat(invite["expires_at"].replace("Z", "+00:00"))` — wenn Supabase je `timestamp` ohne TZ returnt → naive datetime → Vergleich mit `datetime.now(timezone.utc)` wirft `TypeError` → 500 statt Reject |
| 259 | `backend/app/rag.py:1162` `enrich_with_neighbors` | Keine Upper-Bound auf `chunk_index` (nur `< 0` rejected) — +1-Query returnt empty für letzten Chunk, aber wirft unnötigen Round-Trip pro Top-N-Hit am Dokument-Ende |
| 260 | `backend/app/main.py:175–181` | CORS `allow_credentials=True` ist Dead-Weight (Frontend nutzt nur Bearer-Headers, kein `credentials: 'include'` irgendwo) + zukünftig risky wenn jemand `*`-Origin setzt |
| 261 | `backend/app/auth.py:53–60` `decode_token` | Swallowt alle `JWTError`-Typen in einer einzigen 401-Message — Client kann nicht expired vs malformed unterscheiden → silent Refresh-Roundtrip auf jeden malformed-Token |
| 262 | `backend/app/main.py:1509` + `image_gen.py:258, 328, 332` | **HTTPException-Detail-Strings mischen Locales** — manche Deutsch, manche Englisch. CLAUDE.md i18n-Regel: alle user-facing Strings müssen i18n-routable sein |
| 263 | `backend/app/encryption.py:13–15` | Reaffirm #9: Fernet-Key aus `JWT_SECRET` im Import — JWT-Rotation (empfohlen quarterly) bricked alle Provider-Keys silent. Sollte eigenes `ENCRYPTION_KEY` env var sein |

### 🟢 Neu Hinweise (4)

- 264 — `app_image_style_presets` `updated_at` ohne Trigger; `image_gen.py:488` setzt manuell, aber Admin-Endpoints `main.py:2440–2462` nicht verifiziert
- 265 — `20260214_initial_schema.sql:16–33` legt `conversations` und `messages` Tabellen an, die nirgends gelesen/geschrieben werden — Phase A replaced sie durch `chats`/`chat_messages` in `20260215_phase_a:3,16`. Dead-Schema
- 266 — `image_gen.py:222` stuft `response.json()` in `exc._provider_body` — wenn FastAPI-Debug-Mode je aktiv: gleiches Leak-Risk wie #235/#255
- 267 — `_extract_*_text` exception messages leaken Library-Paths (`PdfReadError`, `BadZipFile`, `xlrd.XLRDError`-Internals) — überlappt #90 aber mit broader Surface

## Status Master-Liste

| Metrik | Vor R11 | Refutierungen | Neu | Nach R11 |
|---|---|---|---|---|
| Total | 234 | −3 | +11 | **~242** |
| Kritisch | 63 | −3 | +3 | **63** |
| Hoch | 8 | — | — | 8 |
| Worth-fixing | 126 | — | +8 | 134 |
| Hinweise | 50 | — | +4 | 54 |

### Cumulative

| Runde | Gefunden | Refutiert | Neu kritisch |
|---|---|---|---|
| 1 | 15 | — | 8 |
| 2 | 13 | — | 4 |
| 3 | 25 | — | 8 |
| 4 | 25 | — | 7 |
| 5 | 18 | — | 6 |
| 6 | 14 | — | 3 |
| 7 | 22 | — | 4 |
| 8 | 28 | — | 8 |
| 9 | 20 | — | 8 |
| 10 | 23 | — | 6 |
| **11** | **11 (+ 3 refutiert)** | **3** | **3** |

### Wichtigstes Signal aus Runde 11

**Erstmals Refutierungen.** Drei R10-Findings (#231 PGRST203, #232 IDOR, #233 wrong user_id) wurden von beiden impartial-Verifizierern unabhängig refutiert. Das bedeutet:

1. **Sweep-Sweet-Spot ist hinter uns** — die echten Findings werden seltener, False-Positive-Rate steigt
2. **Sweeper greifen jetzt Schein-Bugs** — wir nähern uns dem Ground-Truth-Limit der Bug-Density
3. **Verifizierungs-Discipline zahlt sich aus** — ohne die Round-11-Refutierungen wären 3 falsche Kritisch-Einträge in den Plan gelandet

### Pattern-Verteilung in R11

| Pattern | R11-Funde |
|---|---|
| P2 F-String / regex bugs | #253 (date regex) |
| P5 Error-Body-Leak | #255 (non-stream pendant zu #235) |
| P7 Pydantic / Schema | #256 (NULL-distinct), #257 (no FK) |
| P8 Silent fallback | #254 (continue-instead-of-break) |
| Wirklich neu | #258 (tz-fragility), #261 (JWTError-types) |

### Empfehlung für Runde 12

Wahrscheinlicher Ertrag: **8–15 weitere Findings** (kalibriert nach R11-Realität). Davon vermutlich **40–60% False Positives**. Trefferquote-für-Aufwand kippt jetzt.

Fokus-Bereiche werden minimal:
- Migration-Reste (`20260214_initial_schema` Dead-Code-Aufräumung)
- Verbleibende Pydantic-Models ohne `extra='forbid'`
- Eventuelle weitere Provider-Name-Inkonsistenzen (sehr unwahrscheinlich nach #230)

**Realistische Erschöpfung jetzt klar:** Runde 12 wird die letzte produktive Runde sein. Runde 13 würde wahrscheinlich nur 3–5 echte Findings bringen bei ~50% False-Positive-Rate. **Pattern-Fix-Phase ist überfällig.** Pattern-Sättigung bleibt; Stellen-Sättigung kommt.

---

## Runde 12 — Refutierungen + 7 echte neue Funde

**Sweep-Sweet-Spot ist endgültig hinter uns.** Drei R11-Findings refutiert (genauso viele wie R11 von R10 refutiert hat) plus mehrere Severity-Downgrades.

### Refutierungen aus R11

| # | Status | Begründung |
|---|---|---|
| **#254 build_rag_context continue-vs-break** | **REFUTIERT** | Beide Verifizierer: greedy bin-packing ist **korrekt** — `continue` lässt smaller subsequent chunks ins verbleibende Budget; `break` würde Budget verschwenden. R11 hat „silent rank-inversion" überverkauft |
| **#258 Invite-Expiry TZ-Fragility** | **REFUTIERT** | `.replace("Z","+00:00")` produziert tz-aware datetime; Vergleich gegen `datetime.now(timezone.utc)` korrekt. Postgres `TIMESTAMPTZ` returnt zuverlässig mit Offset |
| **#261 decode_token JWTError-Swallow** | **REFUTIERT** | Catch-all ist **intentional Security-Practice** — Differenzierung wäre Attacker-Hilfe |

### Severity-Downgrades

- #253 (Date day-discard) → LOW cosmetic (Filter ist by-design month-grained)
- #259 (enrich_with_neighbors upper bound) → Perf-Nit, kein Bug
- #260 (CORS allow_credentials) → LOW, Defense-in-Depth-Cleanup
- #262 (Mixed locale) → LOW (UX)

### 🔴 Neu kritisch (2)

| # | Datei:Zeile | Was |
|---|---|---|
| 268 | `backend/app/rag.py:1194–1201` `enrich_with_neighbors` | **Re-sortiert by raw `similarity`** und unrolled damit RRF + Cohere-Rerank-Order silent. Default-aktiviert (`neighbor_chunks_enabled=True`). Kommentar bei `rag.py:940–946` warnt explizit vor genau diesem Anti-Pattern. Fix: Sort-Key aus `_apply_optional_rerank:949–953` spiegeln |
| 269 | Schema `20260216_phase_d_admin_audit.sql:35` + `main.py:1338, 1447, 1464` | **Audit-Inserts gedroppt bei non-UUID `target_id`** — Schema UUID, Code passt Strings `"all"`, `"openai"`, `"mistral"`. Postgres rejected, `audit.py:61` swallowt → Provider-Key-Set/Delete + Rechunk-Start sind **nie audited**. Vertieft #187 |

### 🟡 Neu Worth-fixing (5)

| # | Datei:Zeile | Was |
|---|---|---|
| 270 | `rag.py:1282` `apply_relevance_gate` | Ignoriert `rerank_score` (Cohere-Relevance wasted) + mixt BM25/Cosinus-Skalen (`similarity` wird bei BM25 mit `bm25_score` überschrieben in `:751`) → Threshold 0.35 sinnlos für BM25-Hits |
| 271 | `rag.py:573` + `:336–341` `should_use_image_retrieval` | **Substring-Match** `keyword in q` → `"bild"` matched `"Bildung"`, `"plot"` matched `"explore"`, `"visual"` matched `"visualization"`. Wasted Asset-Embedding-Work |
| 272 | `main.py:192–194` `/api/health` | **Static 200**, prüft nicht Supabase. Coolify zeigt „healthy" während Backend 500't. Kein `/api/ready` |
| 273 | `main.py:1926–1934` `upload_pool_text` | **Bypassed Content-Hash-Dedup** — File-Upload bei `:1842` passt `content_hash`, Text-Paste nicht. N Pastes gleichen Texts → N Documents + N Embedding-Runs |
| 274 | `providers.py:13` + `llm.py:18–58` Cohere | `KNOWN_PROVIDERS` listet `cohere` aber `PROVIDER_CONFIG` hat keinen Eintrag. `parse_model_string("cohere/…")` → `Unknown provider`. Admin kann Cohere-Key in UI eingeben (encrypted gespeichert), aber Send-Message 502't |

### 🟢 Neu Hinweise (3)

- 275 — `models.py:58` `LoginRequest.username` unbounded → megabyte-Username in `app_audit_logs.metadata` bei Failed-Login (companion zu #80)
- 276 — `main.py:215, 231, 240` `request.client.host` → AttributeError wenn `request.client` is None (reaffirms #33)
- 277 — `documents.py:753–791` `_mark_recurring_by_phash` O(N²) für N>500 Bilder pro PDF

## Status Master-Liste

| Metrik | Vor R12 | Refutierungen | Downgrades | Neu | Nach R12 |
|---|---|---|---|---|---|
| Total | 242 | −3 | (kein -, nur sev) | +10 | **~249** |
| Kritisch | 63 | −3 (R11) | (kein) | +2 | **62** |
| Hoch | 8 | — | — | — | 8 |
| Worth-fixing | 134 | — | +R11-#260/#262 hierher | +5 | 139 |
| Hinweise | 50 | — | +R11-#253/#259 hierher | +3 | 53 |

### Cumulative-Trend

| Runde | Neu | Refutiert | Neu kritisch |
|---|---|---|---|
| 1–10 | 195 | 0 | 64 |
| 11 | 11 | 3 (R10) | 3 |
| **12** | **10** | **3 (R11)** | **2** |

**Klare Sättigung:** R11 und R12 haben identisch viele R-1-Findings refutiert. False-Positive-Rate ist jetzt stabil bei ~30%. Marginale Trefferquote sinkt.

### Pattern-Verteilung in R12

- P2 Substring/Regex: #271
- P8 Silent-Exception-Swallow: #269
- **Unique-Logik-Bugs**: #268 (re-sort), #272 (static health), #273 (text dedup), #274 (Cohere half-wired)

### Empfehlung: STOP — Pattern-Fix-Phase starten

**Runde 13 ist nicht empfohlen.** Erwartet:
- 3–5 neue Findings
- 50–60% False-Positives
- Echte Trefferquote: **1–2 echte neue Findings pro 6 Agenten**

**Pattern-Fix-Phase erschlägt ~55 von 62 kritischen Findings über 5 Pattern-Klassen:**

| Pattern-Fix | Erschlägt Findings | Geschätzter Aufwand |
|---|---|---|
| P1 (Atomicity): RPC + Advisory-Lock + Partial-Unique-Index | ~12 | 3–5 Tage |
| P5 (Provider-Error-Body): generic 502 + Server-Log + Secret-Scrub | ~8 | 1 Tag |
| P9 (Missing-Audit): `target_id TEXT` + Pool-Admin-Audit-Wrapper | ~10 | 2 Tage |
| P11 (Frontend-Auth): AuthContext + Refresh-Mutex + Storage-Event-Sync | ~10 | 4–6 Tage |
| i18n-Sweep (mechanisch) | ~15 | 2–3 Tage |
| **Gesamt Pattern-Fixes** | **~55** | **~14 Tage** |

Plus ~10 wirklich unique-Findings als gezielte Einzel-Fixes (~3–5 Tage):
- #1 supabase-NameError (1 Zeile)
- #178 Admin-Bootstrap
- #230 xAI-Provider-Mismatch
- #268 enrich_with_neighbors re-sort
- #143 image-params-bypass
- #110 rechunk-race
- #142 Pool-Admin-Audit-Gap
- #51 chunk_text infinite-loop
- #53 Prompt-Injection-XML
- #54 XFF-Rate-Limit-Bypass

**Total für alle 62 kritischen Findings: ~3–4 Wochen.**

Bug-für-Bug wären es geschätzt 6–8 Wochen.
